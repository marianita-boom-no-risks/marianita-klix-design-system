## What this PR does

One or two sentences. Be concrete.

## Why

What problem does this solve? Link the issue if there is one.

## How

Quick walk-through of the approach. Anything tricky or non-obvious worth flagging.

## Checks

- [ ] I ran `./install.sh --check` and the local install is healthy
- [ ] If I touched component code, I ran the audit: `./.claude/skills/marianita-klix-ds/scripts/audit-output.sh <file>`
- [ ] If I touched an HTML doc page, I verified both light and dark mode look right
- [ ] If I added a new `mk-` class, I registered it in `mcp/registry.json`
- [ ] If I changed a rule or workflow, I updated `CLAUDE.md`, `SKILL.md`, and the skill's `CHANGELOG.md`

## Screenshots / demo

(If visual.)

## Anything else

Things I'm unsure about, follow-up work, related cleanup that didn't fit here.
